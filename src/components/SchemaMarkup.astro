---
interface Props {
  type: 'Organization' | 'LocalBusiness' | 'FAQPage' | 'Article';
  data: any;
}

const { type, data } = Astro.props;

let schema: any = {};

if (type === 'Organization') {
  schema = {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "Monte-Escalier Vaucluse",
    "url": "https://monte-escalier-vaucluse.fr",
    "logo": "https://monte-escalier-vaucluse.fr/images/logo.png",
    "description": "Service de conseil et devis gratuit pour l'installation de monte-escaliers seniors et PMR en Vaucluse (84).",
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "Avignon",
      "postalCode": "84000",
      "addressRegion": "Provence-Alpes-Côte d'Azur",
      "addressCountry": "FR"
    }
  };
} else if (type === 'LocalBusiness') {
  schema = {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": data.name || "Monte-Escalier Vaucluse",
    "description": data.description || "Installation et devis de monte-escaliers en Vaucluse.",
    "url": data.url || "https://monte-escalier-vaucluse.fr",
    "telephone": data.telephone || "04 90 00 00 00",
    "priceRange": "€€",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": data.street || "",
      "addressLocality": data.locality || "Avignon",
      "postalCode": data.postalCode || "84000",
      "addressRegion": "Vaucluse",
      "addressCountry": "FR"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": data.latitude || 43.9493,
      "longitude": data.longitude || 4.8055
    }
  };
} else if (type === 'FAQPage') {
  schema = {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": data.map((item: any) => ({
      "@type": "Question",
      "name": item.q,
      "acceptedAnswer": {
        "@type": "Answer",
        "text": item.a
      }
    }))
  };
} else if (type === 'Article') {
  schema = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": data.title,
    "description": data.description,
    "image": data.image ? `https://monte-escalier-vaucluse.fr${data.image}` : "https://monte-escalier-vaucluse.fr/images/hero-monte-escalier-vaucluse.png",
    "datePublished": data.publishDate || "2026-07-06",
    "author": {
      "@type": "Person",
      "name": data.author || "Conseiller Mobilité Senior"
    },
    "publisher": {
      "@type": "Organization",
      "name": "Monte-Escalier Vaucluse",
      "logo": {
        "@type": "ImageObject",
        "url": "https://monte-escalier-vaucluse.fr/images/logo.png"
      }
    }
  };
}
---
<script type="application/ld+json" set:html={JSON.stringify(schema)} />
